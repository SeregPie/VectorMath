let {assert} = require('chai');

let VectorMath = require('../../index');

module.exports = function() {
	//assert.almostEqual(VectorMath.covariance([], []));
	//assert.almostEqual(VectorMath.covariance([-64, 58, 57, -43], [-64, 58, 57, -43]));
	//assert.almostEqual(VectorMath.covariance([17, -27, 11, 95], [-47, 5, 5, -48]));
};
